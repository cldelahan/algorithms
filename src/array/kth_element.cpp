#include <iostream>
#include <vector>

#include "kth_element.h"

namespace Array::KthElement
{

    /**
     * @brief Computing the kth element of an array using a deterministic quickselect algorithm
     * 
     * @param vec An vector of floats
     * @return float The k-th element
     */
    float kth_element_deterministic_quickselect(std::vector<float> vec)
    {
        throw std::logic_error("Not yet implemented");
    }

    /**
     * @brief Private methods not for export
     * 
     */
    namespace
    {
        [[maybe_unused]] int partition(std::vector<float> vec, int l)
        {
            throw std::logic_error("Not yet implemented");
        }
    }
}
